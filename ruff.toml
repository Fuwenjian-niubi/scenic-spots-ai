# ruff 配置：本项目为「直连版」演示服务器，以下规则与代码风格不符，予以忽略。
[lint]
ignore = [
    "BLE001",   # 服务器异常兜底：宽泛捕获并返回 JSON 错误（有意为之）
    "S110",     # 可选依赖 pygame 缺失时静默降级为仅保存音频
    "ASYNC230", # demo_voice 边合成边追加音频文件，阻塞写可接受
]
